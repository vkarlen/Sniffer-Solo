import { combineReducers } from 'redux';

const petDetail = (state = {}, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default combineReducers({
  petDetail,
});
